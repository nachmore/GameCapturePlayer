<Window x:Class="GameCapturePlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="Game Capture Player" Height="720" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" x:Name="topBar" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="6,2,6,2">
                <!-- Start/Stop icon button -->
                <Button x:Name="btnStartStop" Width="28" Height="24" Padding="0" Margin="0,0,6,0" Click="btnStartStop_Click" Background="Transparent" BorderThickness="0" ToolTip="Start">
                    <Grid>
                        <TextBlock x:Name="playIcon" Text="&#xf5b0;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="Green" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Ellipse x:Name="stopIcon" Width="14" Height="14" Fill="Red" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                    </Grid>
                </Button>

                <!-- Settings icon (sliders) -->
                <Button x:Name="btnSettings" Width="28" Height="24" Padding="0" Margin="0,0,6,0" Click="btnSettings_Click" Background="Transparent" BorderThickness="0" ToolTip="Settings">
                    <TextBlock Text="&#xE713;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>

                <!-- Fullscreen icon (corner brackets) -->
                <Button x:Name="btnFullscreen" Width="28" Height="24" Padding="0" Margin="0,0,6,0" Click="btnFullscreen_Click" Background="Transparent" BorderThickness="0" ToolTip="Enter fullscreen">
                    <TextBlock Text="&#xE740;" FontFamily="Segoe Fluent Icons" FontSize="14" Foreground="Black" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Margin="0">
            <Border Background="#111">
                <wfi:WindowsFormsHost x:Name="wfHost">
                    <wf:Panel x:Name="videoPanel" BackColor="Black"/>
                </wfi:WindowsFormsHost>
            </Border>
            <!-- Centered fullscreen hint overlay -->
            <Border x:Name="fullscreenHint" HorizontalAlignment="Center" VerticalAlignment="Center" Background="#AA808080" CornerRadius="6" Padding="12" Visibility="Collapsed">
                <TextBlock Text="Press ESC to exit fullscreen mode" Foreground="White" FontSize="16"/>
            </Border>
        </Grid>

        <!-- Hidden selectors preserved for code-behind compatibility; real UI moved to Settings -->
        <Grid Visibility="Collapsed">
            <ComboBox x:Name="cmbVideo"/>
            <ComboBox x:Name="cmbAudio"/>
        </Grid>
    </Grid>
</Window>
