<Window x:Class="GameCapturePlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        xmlns:wfi="clr-namespace:System.Windows.Forms.Integration;assembly=WindowsFormsIntegration"
        mc:Ignorable="d"
        Title="Game Capture Player" Height="720" Width="1280">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="8" Grid.Row="0">
            <StackPanel>
                <TextBlock Text="Video Device"/>
                <ComboBox x:Name="cmbVideo" Width="320" DisplayMemberPath="Name"/>
            </StackPanel>
            <StackPanel Margin="16,0,0,0">
                <TextBlock Text="Audio Device"/>
                <ComboBox x:Name="cmbAudio" Width="320" DisplayMemberPath="Name"/>
            </StackPanel>
            <Button x:Name="btnStart" Content="Start" Width="100" Height="32" Margin="16,0,0,0" Click="btnStart_Click"/>
            <Button x:Name="btnStop" Content="Stop" Width="100" Height="32" Margin="8,0,0,0" Click="btnStop_Click"/>
            <TextBlock x:Name="txtStatus" Margin="16,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="1" Margin="8" Background="#111">
            <wfi:WindowsFormsHost x:Name="wfHost">
                <wf:Panel x:Name="videoPanel" BackColor="Black"/>
            </wfi:WindowsFormsHost>
        </Border>
    </Grid>
</Window>
