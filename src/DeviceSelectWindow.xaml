<Window x:Class="GameCapturePlayer.DeviceSelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Devices" Height="260" Width="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False" Background="#1e1e1e"
        KeyDown="Window_KeyDown">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with title and close button, draggable -->
        <Border Grid.Row="0" Background="#2a2a2a" Padding="12,8" MouseLeftButtonDown="Header_MouseLeftButtonDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Select Devices" Foreground="White" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                <Button Grid.Column="1" x:Name="btnClose" Width="28" Height="24" Background="Transparent" BorderThickness="0" Click="BtnClose_Click" ToolTip="Close">
                    <TextBlock Text="✕" FontSize="14" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Button>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Video device:" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="White"/>
            <ComboBox x:Name="cmbVideo" Grid.Column="1" Width="380" DisplayMemberPath="Name"/>

            <TextBlock Grid.Row="1" Text="Audio device:" VerticalAlignment="Center" Margin="0,8,8,0" Foreground="White"/>
            <ComboBox x:Name="cmbAudio" Grid.Row="1" Grid.Column="1" Width="380" DisplayMemberPath="Name" Margin="0,8,0,0"/>

            <CheckBox x:Name="chkRemember" Grid.Row="2" Grid.ColumnSpan="2" Content="Remember for next time" Margin="0,12,0,0" Foreground="White"/>
        </Grid>

        <!-- Footer -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12,0,12,12">
            <Button Content="OK" Width="90" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" Width="90" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
